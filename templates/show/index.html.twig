{% extends 'base.html.twig' %}

{% block title %}Hello AccueilController!{% endblock %}
{% block body %}
<div class="card" style="width: 18rem;">
  <img class="card-img-top" src="{{article.image}}" alt="{{article.title}}">
  <div class="card-body">
    <h5 class="card-title">{{article.title}}</h5>
    <p class="date">Ajouté le{{article.createdAt | date("d/m/y")}}</p>
    <p class="date">Modifié le{{article.updatedAt | date("d/m/y")}}</p>
    <p class="card-content">{{article.contenu}}</p>
  </div>
</div>
{# </div>
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/symfony/blogger/src/Controller/AccueilController.php'|file_link(0) }}">src/Controller/AccueilController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/symfony/blogger/templates/accueil/index.html.twig'|file_link(0) }}">templates/accueil/index.html.twig</a></code></li>
    </ul>
</div> #}
{% endblock %}